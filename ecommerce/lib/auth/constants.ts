/**
 * Costanti auth prive di dipendenze server (Prisma, next/headers).
 * Importabili anche dal middleware, che gira nell'edge runtime.
 */
export const SESSION_COOKIE = "sessa_admin_session";
export const CUSTOMER_SESSION_COOKIE = "sessa_customer_session";
